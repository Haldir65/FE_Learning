export function getHeaderTitle (state) {
  return state.headerTitle;
};

// export const getHeaderTitle = state => state.headerTitle;
export function getMenuShow (state) {
  return state.menuShow;
};
