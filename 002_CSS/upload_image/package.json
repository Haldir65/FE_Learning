{
  "name": "upload_image",
  "version": "1.0.0",
  "description": "sample app for uploading image to cloud server",
  "main": "app.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "nodemon app.js",
    "production": "pm2 start app.js -i max",
    "stop": "pm2 stop all"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "express": "^4.16.2",
    "formidable": "^1.1.1",
    "nodemon": "^1.14.12",
    "pm2": "^2.9.3"
  },
  "devDependencies": {
    "dotenv": "^5.0.0"
  }
}
